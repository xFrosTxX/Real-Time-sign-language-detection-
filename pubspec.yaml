name: signrecog_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2

  # UI and icons
  cupertino_icons: ^1.0.8

  # Camera and ML libraries
  camera: ^0.11.3
  tflite_flutter: ^0.12.1
  image: ^4.1.3
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
   
